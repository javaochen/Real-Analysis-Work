FDU_实变函数2023版
## 1.1
### 10 证明: g进位有限小数全体和循环小数全体都是可列集
有限小数可以按照（非 0最低位数、最高位数、此时个数）张量的对角线计数或者排列，循环小数按照（有限小数部分，循环节）排列
### 12，求单调递增的正整数列全体的势
二进制小数，值为 1 的位置序号与单调增正整数列一一对应，势为c

### 15，
设{xn}为一实数列. 若它中的元素彼此不同，证明: 它的子序列全体势为 c．若 {xn}中只有有限项彼此不同，那么它的子序列全体的势如何? 说明理由.
1.此时双射于自然数集，所有子序列势为c，除去有限子集全体的  $\aleph_{0}$ ，得到c
2.可数次有限项选择，势为   $\aleph_{0}$ 
——错！ $2^{\aleph_0}=\mathfrak{c}$ ，除非原序列最终为常值
### 16，
1)首先 它们的势不大于 C，如果都小于 C，它们可以与小数集建立双射，他们的积集可以双射为 将它们的小数位交错排列，势就与他们一样，非c，矛盾
2)并集的势不超过各集合势的和， $\mathfrak{c}\leq\sum_{n=1}^\infty\kappa_n$ 
取所有实数序列构成的集合，存在单射f:  $\mathbb{R}^\mathbb{N}\hookrightarrow\bigsqcup_{n=1}^\infty Y_n$ ，右边 分别与 $A_n$ 等势
利用不交并完成对角线，  $C_m\text{ 是 }f^{-1}(Y_m)$ 的第m坐标
若没有一个与 $\mathbb R$ 等势，则在单射意义下  $\exists t_m\in\mathbb{R}\setminus C_m,\quad\forall m\in\mathbb{N}$ 
  $f(t_1,t_2,t_3,\ldots)$   $\in Y_k$ ，则  $t_k\in C_k$ ，矛盾

 

### 18，
a)反设 E 不可数 → 存在 ε>0 使 {x∈E : x≥ε} 仍不可数（否则 E 可数）。 取  $F_n$ = {x∈E : x≥ε} 的前 n 个元素， 则  $s_{F_n}$  ≥ nε → $\sup s_F$ = +∞，矛盾。

故 E 必可数。
b)同理；部分和单调递增，其极限（无论有限或无穷）就是所有有限子集和的上确界。

### 19，
可沿着跳跃量大于1/n数尽所有不连续点

### 20
势 $2^{c}$ 
属于所有从 ℝ 到 ℝ 的函数
在一个不可数集 S ⊂ ℝ 上定义任意实值函数（值域任意），而在其补集（可数集）上定义为连续，从而整体只有可数个不连续点
## 1.2

### 2
它们的并集U是开集
若U非开区间，则 $\exists I_{a},I_{b}\in U,$ 使得无U中的路径将其连通

### 3，
闭集可表示为开集的可数交
闭集F的邻域 $U_n=\{x\in\mathbb{R}:d(x,F)\le\frac1n\}$ ，其中距离  $d(x,F)=\inf\{|x-y|:y\in F\}$ 
$n\to\infty$ , $d(x,F)\to 0$ , $x\in F$ ,则  $\bigcap_{n=1}^\infty U_n=F$ 
利用De Morgan定律，开集是可数个闭集的并集
